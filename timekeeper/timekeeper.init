#!/bin/sh /etc/rc.common
# shellcheck disable=SC2034

START=10
STOP=90
USE_PROCD=1

PROG=/data/timekeeper/timekeeper.sh

start_service() {
    procd_open_instance
    procd_set_param command "$PROG" watch
    procd_set_param stdout 1
    procd_set_param stderr 1
    procd_close_instance
}
